var row = document.getElementById('student_<%= @attendance_record.student_id %>');
row.getElementsByClassName('update_note')[0].style.display = 'block'
setTimeout(function(){
	row.getElementsByClassName('update_note')[0].style.display = 'none'
	},500);
